<Window x:Class="Cashlab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cashlab" 
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Window.DataContext>
        <local:CashViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1">
            <!--<ListBox ItemsSource="{Binding Cashes}" Margin="10" Height="400">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Касса - "/>
                                <TextBlock Text="{Binding Path = Id}"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Количество человек в очереди - "/>
                                <TextBlock Text="{Binding Path = ClientCount}"/>
                            </StackPanel>
                        </StackPanel>
                        
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>-->
            <StackPanel DataContext="{Binding SelectCash}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Касса - "/>
                    <TextBlock Text="{Binding Path = Id}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Количество человек в очереди - "/>
                    <TextBlock Text="{Binding Path = ClientCount}"/>
                </StackPanel>
            </StackPanel>
            

        </StackPanel>

    </Grid>
</Window>
